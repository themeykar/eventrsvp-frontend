export const metadata = {
  title: "Log In",
  description:
    "Log in to your EventRSVP account to manage your events and guest RSVPs.",
};

export default function LoginLayout({ children }) {
  return children;
}
